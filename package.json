{
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "eslint './src/**/*.{js,jsx,ts,tsx}'",
    "format": "prettier --write './src/**/*.{js,jsx,ts,tsx,json,css,md}'",
    "prepare": "npm run build",
    "publish": "npm publish --registry http://localhost:8081"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^12.0.0",
    "@types/jest": "^27.5.2",
    "babel-jest": "^x.x.x",
    "eslint": "^7.0.0",
    "jest": "^27.0.0",
    "prettier": "^2.0.0",
    "ts-jest": "^27.0.0",
    "typescript": "^4.0.0"
  }
}
